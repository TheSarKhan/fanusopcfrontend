// Re-export the existing public booking page so it renders inside the
// patient panel shell (sidebar + header) when the user navigates from
// /patient/psychologists.
export { default } from "@/app/book/[slug]/page";
