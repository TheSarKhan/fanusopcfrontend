// Re-export the public psychologist profile page so it renders inside the
// patient panel shell when navigated from /patient/psychologists.
export { default } from "@/app/psychologists/[slug]/page";
